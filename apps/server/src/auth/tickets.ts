import { randomBytes } from 'node:crypto';
export type TicketKind = 'logs'|'terminal'; export type Ticket = { id: string; sessionId: string; kind: TicketKind; target: string; expires: number };
export class TicketStore { private tickets = new Map<string, Ticket>(); constructor(private readonly now: () => number = Date.now) {} mint(sessionId: string, kind: TicketKind, target: string): Ticket { const ticket = { id: randomBytes(24).toString('base64url'), sessionId, kind, target, expires: this.now() + 30_000 }; this.tickets.set(ticket.id, ticket); return ticket; } consume(id: string, sessionId: string, kind: TicketKind, target: string): boolean { const t = this.tickets.get(id); this.tickets.delete(id); return !!t && t.expires >= this.now() && t.sessionId === sessionId && t.kind === kind && t.target === target; } }
