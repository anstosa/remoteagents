// Superseded by src/commands/service.ts — the generic command catalog scanner
// (skills scanned from the Adapter's directories plus its slash list, ADR 0002)
// replaces the Codex-only SkillService. Retained as an empty module because the
// environment blocks deleting tracked files; safe to delete.
export {};
